# ATOP 使用手册

本文档面向平台管理员、项目负责人和日常使用人员，说明 ATOP 的核心使用流程。

## 1. 登录与账号

访问平台地址后进入登录页，使用管理员分配的邮箱和密码登录。

首次部署时的超级管理员账号来自 `backend/deploy/.env`：

- `ADMIN_EMAIL`
- `ADMIN_PASSWORD`

首次登录后请按系统提示修改密码。密码至少 8 位，并包含大写字母、小写字母和数字。

登录后可在右上角用户菜单进入个人设置，维护个人信息和头像。退出登录同样在右上角用户菜单中操作。

## 2. 首页仪表盘

仪表盘用于查看平台运行态势：

- 当前运行中的流水线。
- 流水线运行成功、失败、取消等状态统计。
- 近期执行趋势和关键指标。
- 可进入运行详情查看任务、阶段和日志检查结果。

建议日常值班或项目负责人优先从仪表盘确认是否存在失败、阻塞或长时间运行的任务。

## 3. 运行记录

运行记录展示历史流水线执行结果。

常用操作：

- 按状态、流水线、时间等条件筛选。
- 打开运行详情，查看任务列表、阶段状态、耗时和错误信息。
- 对异常任务执行日志检查。
- 对可重跑的流水线执行重建。
- 对正在运行的流水线或任务执行中止。

运行详情中的任务状态来自 ATOP 调度记录与 Jenkins 回调。若状态长期不更新，请检查 Jenkins 回调地址和网络连通性。

## 4. 流水线管理

流水线是 ATOP 的核心编排单元。进入“流水线”后可以创建、编辑、删除、触发和查看流水线。

创建流水线的一般流程：

1. 填写流水线名称、描述、归属项目等基础信息。
2. 配置可执行任务、参数和 Jenkins 相关信息。
3. 如果需要复杂依赖，进入 DAG 编排视图配置节点和依赖关系。
4. 保存后可进行运行校验。
5. 在流水线列表或详情中触发运行。

触发运行时可选择环境、变量、测试集或其他运行参数。提交后进入运行详情查看实时状态。

## 5. DAG 编排

DAG 编排用于描述多任务之间的依赖关系，适合以下场景：

- 多个测试任务并行执行。
- 上游任务成功后再触发下游任务。
- 子流水线组合运行。
- 部分节点失败时按策略继续、停止或汇聚。

使用建议：

- 节点名称保持清晰，便于在运行详情中定位。
- 依赖关系从左到右或从上到下组织，减少交叉线。
- 保存前使用校验能力检查是否存在环形依赖或缺失参数。
- 修改 DAG 后建议先用小范围测试集试运行。

## 6. 流水线版本

每次关键修改后可通过版本记录查看差异，并在需要时恢复历史版本。

适用场景：

- 排查某次配置变更导致的运行失败。
- 对比任务参数、DAG 或访问控制变化。
- 快速回滚到稳定配置。

恢复历史版本前请确认当前草稿或未保存改动已经处理。

## 7. Jenkins 集成

进入“Jenkins 实例”维护 Jenkins 连接信息。

配置步骤：

1. 新增 Jenkins 实例，填写名称、地址、账号或 Token。
2. 点击连通性测试，确认 ATOP 后端可以访问 Jenkins。
3. 同步 Agent 节点和标签。
4. 在流水线或任务中选择对应 Jenkins 实例、Job、Agent 标签和运行参数。
5. 确认 Jenkins 能访问 `WEBHOOK_BASE_URL`，否则任务结果无法回传 ATOP。

安全建议：

- 使用 Jenkins 专用账号或 API Token。
- 只授予必要的 Job 读取、构建和节点查询权限。
- 定期轮换 Token，并在 ATOP 中同步更新。

## 8. 公共变量与数据字典

公共变量用于统一管理流水线中复用的键值参数，例如环境地址、账号别名、构建参数等。

数据字典用于维护可选维度，例如项目、环境、产品、系统、运行类型等。规范数据字典能让筛选、报表和触发参数更一致。

维护建议：

- 变量命名使用清晰前缀，例如 `dev.api_base_url`。
- 不要把明文密码放入普通公共变量。
- 删除或改名变量前确认没有流水线依赖。
- 数据字典停用优先于删除，避免影响历史记录理解。

## 9. 测试集与批量导入

测试集用于组织可被流水线引用的测试数据或测试目标。

常用能力：

- 创建、编辑、启用和停用测试集。
- 克隆已有测试集。
- 批量导入测试集。
- 预览测试集 JSON。
- 使用标签和维度筛选。

批量导入前建议先使用预览，确认字段映射、必填项和枚举值符合平台要求。

## 10. 通知中心与通知规则

通知中心展示平台内消息，例如任务结果、系统提醒、审批或运维通知。

通知规则用于配置在特定事件发生时如何通知用户：

- 选择触发事件。
- 设置接收人或角色。
- 选择站内信、邮件或其他通道。
- 使用测试发送验证配置。

如果需要邮件通知，部署时必须配置 SMTP。忘记密码邮件也依赖 SMTP。

## 11. 用户、角色与权限

平台内置角色：

- 超级管理员：拥有全部权限。
- 项目负责人：管理测试集、流水线等核心资源。
- 普通成员：查看和触发运行。
- 访客：以查看为主。

管理员可以：

- 创建、编辑、停用用户。
- 批量导入用户。
- 重置用户密码。
- 维护角色。
- 配置角色权限矩阵。
- 为单个用户设置权限覆盖。
- 查看用户或角色变更历史。

权限变更后，用户可能需要刷新页面或重新登录才能看到最新菜单和操作权限。

## 12. 审计日志

审计日志记录关键操作，包括用户、动作、资源、时间、IP 和操作入口等信息。

常见用途：

- 追踪配置被谁修改。
- 排查误操作。
- 导出合规记录。
- 复盘故障前后的操作路径。

建议管理员定期检查高风险操作，例如删除、重置密码、权限变更和数据清理。

## 13. 数据清理

数据清理用于控制历史运行记录、日志和临时数据的保留周期。

操作建议：

- 先执行 dry-run 查看预计影响范围。
- 在低峰时段执行正式清理。
- 清理前完成数据库备份。
- 对合规或审计需要长期保留的数据，先确认保留策略。

## 14. 版本中心与文档中心

版本中心用于查看平台版本信息和发布记录。文档中心用于集中查看平台说明、制度或操作文档。

管理员可结合团队流程，把部署记录、使用规范、变更公告等内容整理到文档中心，方便使用人员查阅。

## 15. 常见使用问题

### 登录后看不到某些菜单

通常是权限不足。联系管理员检查角色权限或用户权限覆盖。

### 触发流水线后一直排队

检查 Jenkins 实例是否可用、Agent 是否在线、Job 是否存在，以及 Jenkins 凭据权限是否足够。

### 流水线已在 Jenkins 结束，但 ATOP 状态未更新

检查 `WEBHOOK_BASE_URL` 是否能从 Jenkins 访问，并确认 Jenkins 脚本是否正确上报 `platform_webhook_url`。

### 忘记密码

在登录页点击忘记密码。如果没有收到邮件，请联系管理员确认 SMTP 配置和邮箱地址。

### 页面提示无权限

当前账号没有执行该操作的权限。联系管理员调整角色权限或申请对应资源权限。
